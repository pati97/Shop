@model Shop.Models.Product

@{ 
    ViewBag.Title = "Index";
}

    <h2>@Model.Name</h2>
    <hr />
<p>
    @Html.ActionLink("Create New", "Create", new { productId = Model.ID})
</p>

@Html.Partial("_Review", Model.Reviews)

@Html.ActionLink("Go back to product list","Index", "Products")